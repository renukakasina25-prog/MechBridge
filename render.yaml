services:
  - type: web
    name: mechbridge
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    staticPublicPath: dist
    envVars:
      - key: NODE_ENV
        value: production
